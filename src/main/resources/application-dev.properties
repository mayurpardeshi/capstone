# --- Database (Local MySQL) ---
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/desicart
spring.datasource.username=mayur_pardeshi
spring.datasource.password=Dista123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA Settings (Dev-friendly) ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Logging (Verbose) ---
logging.level.org.springframework.web=TRACE
logging.level.org.springframework.security=DEBUG

baseURL=http://localhost:8081/

razorpay.key.id=rzp_test_Rygdi2QEuUjTJg
razorpay.key.secret=PI5sAOy93MSB3ejWBb8Kv03H
razorpay.webhook.secret=DesiCartSecret2026

# Connect to the Docker container
spring.kafka.bootstrap-servers=localhost:9092

# Serialization settings for your Event object
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer

spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer

# Important: Allow your package to be trusted for JSON deserialization
spring.kafka.consumer.properties.spring.json.trusted.packages=com.mayur.DesiCart.*

management.endpoints.web.exposure.include=health,info,beans,env,metrics,httpexchanges,loggers