# --- General Metadata ---
spring.application.name=DesiCart
server.port=8081
api_prefix=api/v1

# --- Documentation (Swagger) ---
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=alpha

# --- Actuator (Monitoring) ---
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always

# --- Default Profile ---
# If no profile is specified, default to 'dev'
spring.profiles.active=dev

baseURL=http://localhost:8081/



# 30 days in milliseconds
app.jwtExpirationMs=2592000000
# Fixed secret so restarts don't invalidate tokens
app.jwtSecret=DesiCart_secret_key

STRIPE_SECRET_KEY= <STRIP_SECRET_KEY>

razorpay.key.id=test_Key
razorpay.key.secret=testingSec
razorpay.webhook.secret=DesiCartSecret2026



# Connect to the Docker container
spring.kafka.bootstrap-servers=localhost:9092

# Serialization settings for your Event object
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer

spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer

# Important: Allow your package to be trusted for JSON deserialization
spring.kafka.consumer.properties.spring.json.trusted.packages=com.mayur.DesiCart.*